# 🇹🇭 โครงการ: การจำแนกภาษามือตัวเลขไทยด้วย MobileNetV2 และ MediaPipe

โครงการนี้ใช้เทคนิค Computer Vision และ Deep Learning เพื่อตรวจจับและจำแนกท่าทางภาษามือตัวเลขไทย (0-9) แบบเรียลไทม์ โดยใช้ MobileNetV2 เป็นโมเดลหลัก

---

### 🚀 ขั้นตอนการติดตั้งและการใช้งาน (Getting Started)

#### 1. การตั้งค่าสภาพแวดล้อม (Conda Setup)

เนื่องจากปัญหาความขัดแย้งของเวอร์ชันไลบรารีที่เคยเกิดขึ้น จึงแนะนำให้ใช้ **Conda Environment** และ **Python 3.11** เพื่อความเสถียรสูงสุด:

1.  **ติดตั้ง Conda** (หากยังไม่มี)

2.  **สร้างและเปิดใช้งาน Environment ใหม่:**
    ```bash
    conda create -n tsl_env python=3.11 -y
    conda activate tsl_env
    ```

3.  **ย้ายไปยังโฟลเดอร์โปรเจกต์:** (เข้าสู่โฟลเดอร์ `src`)
    ```bash
    cd src
    ```

#### 2. การติดตั้งไลบรารี (Dependencies)

เมื่ออยู่ใน `(tsl_env)` แล้ว ให้ติดตั้งไลบรารีที่จำเป็นทั้งหมดในชุดที่เข้ากันได้:

```bash
# ติดตั้งไลบรารีทั้งหมดที่จำเป็น: TensorFlow, MediaPipe, OpenCV, และเครื่องมือวิเคราะห์
pip install tensorflow mediapipe opencv-python numpy scikit-learn seaborn matplotlib

หากมีการเพิ่มรูปภาพใหม่ในโฟลเดอร์ dataset/numbers/ ให้รันสคริปต์นี้เพื่อแบ่งข้อมูล (80% Training, 10% Validation, 10% Testing):

Bash
(tsl_env) terspn@... src % python 00_split_data.py
ขั้นตอนที่ 2: การฝึกฝนโมเดล (Model Training)

รันสคริปต์นี้เพื่อเริ่มฝึกโมเดล MobileNetV2 (ใช้เทคนิค Fine-tuning ที่มีการตั้งค่าไว้แล้ว):

Bash
(tsl_env) terspn@... src % python 01_train_model.py
ผลลัพธ์: ไฟล์โมเดล thai_sign_numbers_cnn.h5 จะถูกบันทึกในโฟลเดอร์ models/

ขั้นตอนที่ 3: การประเมินผล (Evaluation)

ใช้สคริปต์นี้เพื่อตรวจสอบความแม่นยำสุดท้ายของโมเดล และดู Confusion Matrix:

Bash
(tsl_env) terspn@... src % python 03_test_final_model.py
ขั้นตอนที่ 4: การทดสอบแบบเรียลไทม์ (Real-time Testing)

รันสคริปต์นี้เพื่อเปิดกล้องเว็บแคมและทดสอบการจำแนกภาษามือตัวเลขแบบสดๆ:

Bash
(tsl_env) terspn@... src % python 02_realtime_app.py